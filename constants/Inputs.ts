import { StyleSheet } from 'react-native';

export const Inputs = StyleSheet.create({
	input: {
		height: 40,
		marginVertical: 12,
		borderWidth: 0.5,
		borderRadius: 4,
		paddingVertical: 12,
		paddingHorizontal: 15,
	  },
});
