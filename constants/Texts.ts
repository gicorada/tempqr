import { StyleSheet } from 'react-native';

export const Texts = StyleSheet.create({
  text: {
    fontSize: 16,
    lineHeight: 21,
    fontWeight: '500',
    color: 'black',
  },
  bold: {
    fontWeight: 'bold',
  }
});
